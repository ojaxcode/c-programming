#include <stdio.h>
int main()
{

    int r = 6;
    int h = 4;

    printf("area of this circle with radius %d is %f\n", r, 3.14*r*r);
    printf("volume of this cylinder with radius %d and height %d is %f\n", r, h, 3.14*r*r*h);
    return 0;
}